{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Smart contracts for admin dashboard",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile --show-stack-traces",
    "test": "hardhat test --network hardhat",
    "deploy:goerli": "hardhat run ./scripts/deploy.ts --network goerli"
  },
  "author": "AndriyAntonenko",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "dotenv": "^16.0.2",
    "hardhat": "^2.13.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.2",
    "@openzeppelin/test-helpers": "^0.5.16"
  }
}
